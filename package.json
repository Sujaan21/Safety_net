{
  "name": "safetynet",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node serve.js",
    "deploy:cloudrun": "gcloud run deploy safetynet --source . --region us-central1 --allow-unauthenticated",
    "deploy:appengine": "gcloud app deploy app.yaml",
    "deploy:firebase": "npx firebase-tools deploy"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  }
}